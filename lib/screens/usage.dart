import 'package:flutter/material.dart';
import 'utils.dart';

class UsageScreen extends AppScreen {
  const UsageScreen({super.key});

  @override
  final title = "Usage";

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("UsageScreen"));
  }
}
